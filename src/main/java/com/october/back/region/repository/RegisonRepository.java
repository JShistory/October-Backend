package com.october.back.region.repository;

import com.october.back.region.entity.Region;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegisonRepository extends JpaRepository<Region, Long> {
}
